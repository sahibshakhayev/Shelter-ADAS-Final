// CustomDashboard.js
import * as React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const CustomDashboard = () => (
    <Card>
        <CardContent>
            <Typography variant="h5">Welcome to Shelter Admin Panel</Typography>
        </CardContent>
    </Card>
);

export default CustomDashboard;
